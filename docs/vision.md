# miniKMS Vision

miniKMS aims to be the one‑stop, multi‑provider KMS emulator for developers.

It provides a clean core engine with pluggable provider modes that mimic AWS KMS, GCP KMS, Azure Key Vault, and Vault Transit.
The goal is not to replace real cloud KMS systems, but to make local development and CI fast, deterministic, and cloud‑agnostic.
